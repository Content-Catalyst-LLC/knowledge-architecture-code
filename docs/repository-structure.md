# Repository Structure

The Knowledge Architecture repository is organized as a reproducible research and publishing-support environment. It separates article-level assets from shared data, schemas, ontology models, taxonomy files, notebooks, and governance documentation.

The article map is implemented directly in `articles/` using one folder per article slug. Section-level index files live in `article-map/`.

