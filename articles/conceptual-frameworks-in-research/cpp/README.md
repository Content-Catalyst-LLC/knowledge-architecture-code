# C++ Scaffold

Efficient edge-list and relationship-count utility scaffold.

Compile and run:

```bash
g++ -std=c++17 framework_relationship_count.cpp -o framework_relationship_count
./framework_relationship_count
```
