# C++

Efficient discrete-structure example for parent-child taxonomy relationships.

Compile from `cpp/`:

```bash
c++ -std=c++17 taxonomy_parent_counts.cpp -o taxonomy_parent_counts
./taxonomy_parent_counts
```
