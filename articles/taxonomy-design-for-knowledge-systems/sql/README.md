# SQL

This folder contains a taxonomy and controlled-vocabulary schema plus sample quality queries. It is designed for SQLite-style prototyping but can be adapted to PostgreSQL or another relational database.
