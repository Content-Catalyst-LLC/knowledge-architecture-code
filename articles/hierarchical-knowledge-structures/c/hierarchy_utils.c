#include <stdio.h>

int main(void) {
    const int node_count = 10;
    const int edge_count = 9;
    const int root_count = 1;
    const int leaf_count = 6;

    printf("C hierarchy utility scaffold\n");
    printf("Node count: %d\n", node_count);
    printf("Edge count: %d\n", edge_count);
    printf("Root count: %d\n", root_count);
    printf("Leaf count: %d\n", leaf_count);

    return 0;
}
